document.querySelector('#print').addEventListener('click',()=>window.print());
document.querySelector('#download').addEventListener('click',()=>{const headers=['Estimate ID','Owner','Estimate sent date','Last contact date','Status','Customer question (no personal details)','Next action','Next action date','Outcome','Booked value','Collected revenue'];const csv=headers.join(',')+'\r\n'+Array(5).fill(Array(headers.length).fill('').join(',')).join('\r\n');const url=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download='midsize-ai-estimate-follow-up-tracker.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});
document.querySelectorAll('[data-copy]').forEach(button=>button.addEventListener('click',async()=>{const text=document.getElementById(button.dataset.copy).textContent;try{await navigator.clipboard.writeText(text);document.querySelector('#copy-status').textContent='Message copied. Replace bracketed details and review before sending.';}catch{document.querySelector('#copy-status').textContent='Select the message text and copy it using your device.';}}));