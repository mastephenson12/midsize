const form=document.querySelector('#signup'),direct=document.querySelector('#direct'),status=document.querySelector('#status');
fetch('/api/contractor-kit').then(r=>r.ok?r.json():null).then(data=>{if(data?.ready===true){form.hidden=false;direct.hidden=true}}).catch(()=>{});
form.addEventListener('submit',async e=>{e.preventDefault();if(!form.reportValidity())return;const button=form.querySelector('button');button.disabled=true;status.textContent='Submitting…';const fields=new FormData(form);try{const r=await fetch('/api/contractor-kit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({firstName:fields.get('firstName'),email:fields.get('email'),consent:fields.get('consent')==='on',companyWebsite:fields.get('companyWebsite'),source:'estimate-follow-up-kit',referrer:new URLSearchParams(location.search).get('utm_source')||'website'})});const data=await r.json();if(!r.ok||!data.ok)throw Error(data.error||'Please try again.');location.assign('/estimate-follow-up-kit/download/');}catch(error){status.textContent=error.message+' You can also read the kit without subscribing below.';button.disabled=false;}});
